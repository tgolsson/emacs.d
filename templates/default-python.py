#!/usr/bin/env python3
# `(upcase (file-name-nondirectory (file-name-sans-extension (buffer-file-name))))` --- Summary
#
# Author: `(if (s-blank? user-full-name) user-login-name user-full-name)` <`(concat user-mail-address)`>
# Copyright © `(format-time-string "%Y")`, `(if (s-blank? user-full-name) user-login-name user-full-name)`, all rights reserved.
# Created: `(format-time-string "%e %B %Y")`


'''
$1
'''

$0
